import 'package:flutter/material.dart';

class MainVendorSettings extends StatelessWidget {
  const MainVendorSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
